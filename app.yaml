runtime: python312
instance_class: F1
entrypoint: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT

env_variables:
  # Environment variables for your app
  LOG_LEVEL: "INFO"
  
handlers:
- url: /.*
  script: auto
